[project]
name = "worker"
version = "1.0.0"
description = "Stand-in for the replenishment worker"
requires-python = ">=3.9"
dependencies = [
    "qb==1.6.1",
]

[build-system]
requires = ["uv_build>=0.11,<0.13"]
build-backend = "uv_build"
