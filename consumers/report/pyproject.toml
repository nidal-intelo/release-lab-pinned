[project]
name = "report"
version = "1.0.0"
description = "Stand-in for report-builder"
requires-python = ">=3.9"
dependencies = [
    "qb==1.5.0",
    "solver==2.0.0",
]

[build-system]
requires = ["uv_build>=0.11,<0.13"]
build-backend = "uv_build"
